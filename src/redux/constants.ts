import createTypes from '$utils/createTypes';

export default createTypes({
  // User actions
  CHANGE_L_PANEL: null,

  // Project actions
  CREATE_PROJECT: null,
  DEL_PROJECT: null,
  SET_PROJECTS: null,

  // Screen actions
  ADD_SCREEN: null,
});
